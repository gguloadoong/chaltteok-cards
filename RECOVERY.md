# @fit.ttok 쓰레드 계정 복구 런북 — 2026-08-25

> 이 문서는 클라우드 Claude Code 세션이 `chaltteok-cards` 리포의 상태판(git 히스토리)·카드 파일을
> 분석해 작성한 인수인계 문서다. 실제 파이프라인(`auto/state.json`, `auto/run.log`, 슬롯 스크립트)은
> 맥 로컬에 있으므로, **맥에서 Claude Code를 열어 이 문서를 읽히고 그대로 실행**하면 된다.

---

## 0. TL;DR

- 8/21까지의 구 체제(4인)는 사실 잘 돌고 있었다: 일 조회 2,000~4,800, 팔로워 0→8 성장 중.
- 8/22 "이상한 글" 사고의 실제 원인은 **① 이미지 파일명 재사용+캐시로 인한 중복 첨부**와
  **② 내부 성과 보고(조회수 복기)가 공개 발행 레인으로 유출**된 것. 콘텐츠 시스템 전체의 문제가 아니었다.
- Codex의 수정은 원인 2개를 고치는 대신 **팀 전면 재설계(8인) + fail-closed 전면 검수 + 발행 잠금 +
  소수 레인 승인**으로 과교정했다. 그 결과 발행량이 일 15장+ → 1장 → 0장으로 붕괴했고,
  조회수 3,849(8/20 발행분) → 917(8/24 발행분), 품질도 깨진 렌더링이 나가는 상태가 됐다.
- 처방: 두 개의 진짜 버그를 **정밀 타격**으로 고정하고, 슬롯·레인은 구 체제 수준으로 복원한다.
  검수는 유지하되 "핵심 3항목만 hard-block, 나머지는 경고 후 발행"으로 되돌린다.

---

## 1. 근거 타임라인 (이 리포에서 재구성)

| 날짜(발행분) | 쓰레드 조회(다음날 집계) | 팔로워 | 카드 업로드 | 비고 |
|---|---|---|---|---|
| 08/19 | 3,849~4,861 (08/20 집계) | 3 | 25개 | 구 체제 최고점 |
| 08/20 | 704~903 (08/21 집계) | 4 | 18개 | 정상 변동 |
| 08/21 | 3,849 (08/21 23:21 집계) | 8 | 15개 | 구 체제 마지막 정상일 |
| 08/22 | (상태판 공백) | — | 4개 | 한도초과·Codex 작업 시작 |
| 08/23 | (상태판 공백) | — | 1개 (깨진 렌더링) | Codex 체제 |
| 08/24 | 917 (08/25 01:04 집계) | 11 | 0개 | 발행 붕괴 |

- 상태판 갱신 커밋이 8/21 23:21(`e7396e5`) 이후 8/25 01:04(`287d272`)까지 완전 공백 —
  이 구간이 한도초과 + Codex 개입 구간.
- 8/25 01:04 상태판의 한입 점검 결과(8/24 23:51): **"poster: 오늘 실패 7회; newswatch: 오늘 실패
  33회; reeler: 실패 2회; analyst: 종료코드 1; council: 실패 2회; serial: 종료코드 1; 오류 12건;
  [필수 슬롯 미발행] 국장 개장 전(08:20)"** + 23:56 "이상 발견 16건".
- 오늘 슬롯 로그: 23:15 슬롯이 00:09/00:24/00:30 세 번 실행(2회 bad), 00:13 "글 작성 실패",
  00:24 "근거 실패(LG화학)". 슬롯 지연 54분 + 중복 재시도.

## 2. "이상한 글" 사고의 진짜 원인 (물증 있음)

### 2-1. 같은 이미지가 다른 글에 계속 붙는 버그 — 원인 2개가 겹침

**(a) 동일 이미지가 다른 파일명으로 중복 첨부** — 이 리포에 바이트 단위 동일 파일이 3일치 남아 있다:

```
md5 d3b3f827…  cards/2026-08-21/card_us_open.png == chart_us_open.png
md5 20112b0c…  cards/2026-08-20/card_us_mid1.png == chart_us_mid1.png
md5 bde8ae41…  cards/2026-08-19/stock_us_close.png == stock_us_pre.png   ← 프리장 이미지가 마감글에 재사용!
```

생성 단계가 실패하면 직전 파일을 복사해 다른 이름으로 재사용하는 폴백이 있는 것으로 보인다.
특히 8/19 건은 프리장 카드가 마감 글에 그대로 나간 사례라 "다른 글에 같은 이미지"의 직접 물증.

**(b) 고정 파일명 덮어쓰기 + CDN 캐시** — `news.png`, `moment.png`, `own.png` 등을
하루에 2~3번 같은 경로에 덮어쓴다 (8/21: news.png 14:19 → 17:00 → 18:00 커밋 3회).
GitHub Pages는 `max-age=600` CDN 캐시가 있어, 덮어쓴 직후 발행하면 쓰레드/인스타 서버가
**이전 글의 이미지를 캐시로 받아간다.** → 새 글에 예전 이미지가 붙는다.

**고정 방법 (파이프라인 수정):**
1. 카드 파일명에 슬롯+타임스탬프(또는 내용 해시)를 넣는다: `news_1419.png`, `stock_kr_close_0825.png`.
   같은 경로 덮어쓰기 전면 금지.
2. 발행 직전, 첨부할 이미지의 md5를 **최근 발행 N건(최소 24시간)의 이미지 md5 목록**과 비교해
   일치하면 발행을 중단하고 로그를 남긴다 (이건 fail-closed가 맞는 지점).
3. 이미지 생성 실패 시 이전 이미지 재사용 폴백을 **삭제**한다. 이미지가 없으면 텍스트만 발행하거나
   해당 슬롯을 건너뛴다 — 낡은 이미지를 붙이는 것보다 낫다.
4. 발행 시 GitHub Pages URL에 캐시버스터 쿼리(`?v=<md5 앞 8자>`)를 붙인다.

### 2-2. "조회수 복기하는 글" — 내부 보고의 공개 레인 유출

구 체제의 눈이(성과 분석)는 매시 "결과보고 630자" 같은 내부 보고를 만들었다(8/21 23:00 로그).
조회수·도달을 복기하는 이 텍스트가 발행 큐로 흘러들어 공개 글로 나간 것이 "조회수 복기 글"의
정체로 추정된다. 8/23의 유일한 카드 `인절미_card.png`도 같은 계열 사고다: **팀원 이름이 파일명**이고,
내용이 "데스크 결론: 0.88%라는 지수 상승을 보고 122개의 하락 종목 수를 두 번 셌어요" 같은
내부 회의 메타 발언이며, 렌더링도 화면 2/3가 빈 미완성 상태로 나갔다.

**고정 방법:**
1. 발행 큐에 들어갈 수 있는 소스를 **레인 화이트리스트**로 제한한다: 시장 슬롯 글, 사건 속보,
   시리즈 글만. 눈이/한입/회의(council) 산출물은 발행 큐 접근 자체를 차단하고
   Discord·상태판으로만 보낸다.
2. 발행 직전 마지막 가드 하나: 본문에 자기 계정 지표 언급(`조회수`, `팔로워`, `도달`, 팀원 이름,
   `데스크`, `회의`)이 있으면 hard-block. 규칙 몇 줄이면 된다.

## 3. Codex 체제가 부러뜨린 것 (과교정 목록)

| 변경 | 결과 | 처방 |
|---|---|---|
| 4인 → 8인 전면 재설계 (콩이 역할까지 변경) | 검증 안 된 새 파이프라인이 실전 투입, 하루 실패 40회+ | 역할은 유지해도 되나 **실행 경로는 구 체제 코드 기반**으로 되돌려 안정화 |
| "승인된 소수 레인"만 발행 | 슬롯 10개 → 4개, 필수 슬롯(08:20)도 미발행 | 구 체제 슬롯 9~10개 전부 복원 |
| 백설기 fail-closed 전면 검수 (원문·날짜·숫자·말투·반복·실명·이미지 7항목 전부 차단) | "근거 실패"·"글 작성 실패" 연발 → 발행 0건 날 발생 | **hard-block은 3개만**: 숫자 사실오류, 원문 URL 부재, 이미지-본문 불일치(§2-1의 md5 가드 포함). 말투·반복 등은 경고 로그 + 발행 진행 |
| 중앙 guard + 발행 잠금 | 슬롯 54분 지연, 3회 중복 재시도 | 잠금은 "동시 발행 1건" 뮤텍스만 남기고 단순화. 재시도는 슬롯당 1회, 성공 시 즉시 종료 |
| newswatch 신설/개편 | 하루 33회 실패 | `auto/run.log`에서 newswatch 스택트레이스 확인 후 근본 수정. 안 되면 구 체제 "20곳 훑음" 점검 코드로 롤백 |

참고: 구 체제에도 이미 좋은 가드가 있었다 — "검수 탈락 [막음]"(수치 불일치 차단, 8/21 22:07),
"전역 간격 60분", "상한 3건 도달 — 쉰다". 전면 재설계가 아니라 이 위에 §2의 두 버그 수정만
얹었으면 됐다.

## 4. 실행 순서 (맥 Claude Code 용)

**P0 — 오늘 발행 재개 (30분)**
1. `auto/run.log`에서 poster 실패 7회·"글 작성 실패"·"근거 실패"의 실제 에러를 읽는다.
2. 백설기 검수를 §3 처방대로 완화 (hard-block 3개, 나머지 경고).
3. 슬롯 테이블을 구 체제 10개로 복원: 01:00, 03:00, 05:10, 08:20, 08:50, 10:30, 13:30, 15:40,
   22:00, 23:15. 스케줄러 지연·중복 재시도 수정.
4. 다음 슬롯에서 발행 1건 성공하는 것 확인.

**P0 — 재발 방지 2건 (1시간)**
5. §2-1 이미지 수정 4개 (파일명 유니크, md5 중복 가드, 폴백 삭제, 캐시버스터).
6. §2-2 레인 화이트리스트 + 자기지표 언급 차단.

**P1 — 안정화 (오늘 중)**
7. newswatch 33회 실패 근본원인 수정 또는 롤백.
8. analyst·serial 종료코드 1 원인 수정.
9. 상태판 커밋이 다시 2~5시간 간격으로 올라오는지 확인 (8/25 01:04 이후 멈춰 있음).

**P2 — 품질·성장 복원 (이번 주)**
10. 잘 먹혔던 포맷으로 복귀: 8/19~21 스타일 — 종목명 + 큰 숫자 하나 + 한 줄 맥락 카드,
    versus 비교 카드(8/22 SK하이닉스 vs 삼성전자 — 이건 Codex 기간에도 좋았다), 밈·시리즈 헤더.
    "찰떡 데스크"류 내부 회의 컨셉 카드는 공개 발행 중단.
11. 인절미 실험은 하루 1개, 단 발행 큐가 아니라 **기존 레인의 A/B**(훅 문장, 첫 이미지)로 제한.
12. 눈이의 성과 분석은 "다음 글 소재 선택"에만 반영 (1천뷰당 참여 기준) — 공개 글 금지 유지.

**검증 기준 (일주일):** 일 발행 6건+, 실패율 <20%, 중복 이미지 0건, 자기지표 언급 글 0건,
일 조회 2,000 회복.

---

## 5. 별건 — 디스코드 무응답

원인은 파이프라인과 무관한 **계정 사용 한도 초과로 Claude 세션이 멈춘 것**
(형제 클라우드 세션 "디스코드 매매 알리미 응답 없음"이 이미 진단). 한도는 8/25 09:00 KST에 리셋.
해당 세션이 `claude-discord-bridge`의 `claude/discord-trading-alert-no-response-rbfvwv` 브랜치에
한도 감지·복구 안내(`doctor.js` 등, +904줄)를 만들어 뒀지만 **main에 미머지·미배포** 상태다.
맥에서 브릿지를 쓰려면 이 브랜치를 검토·머지하고 `start.sh` 재시작.
